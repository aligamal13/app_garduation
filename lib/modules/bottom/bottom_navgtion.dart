import 'package:flutter/material.dart';

class BottomNavgtion extends StatelessWidget {
  const BottomNavgtion({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
